# Core API
fastapi
uvicorn[standard]
pydantic>=2.0.0
python-dotenv

# LLM (Gemini)
google-generativeai

# HTTP client (used by OCR + HF router)
requests

# Web search (SerpAPI)
google-search-results

# Database (Supabase)
supabase

# JWT signature verification (Supabase auth tokens)
PyJWT

# Rate limiting
slowapi

# OCR / screen capture (Windows)
pytesseract
opencv-python
numpy
mss
pywin32

# Testing
pytest
httpx

# Optional LLM providers (install as needed)
# anthropic       # for Claude provider
# openai          # for OpenAI provider

# Sentry (optional observability)
# sentry-sdk[fastapi]
