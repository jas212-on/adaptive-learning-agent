# Core API
fastapi
uvicorn[standard]
pydantic
python-dotenv

# LLM (Gemini)
google-generativeai

# HTTP client (used by OCR + HF router)
requests

# Web search (SerpAPI)
google-search-results

# OCR / screen capture (Windows)
pytesseract
opencv-python
numpy
mss
pywin32
